org.gradle.jvmargs=-Xmx4096m
android.useAndroidX=true
# Quest 2/3 are arm64 only
ABI=arm64-v8a
# Path to your vcpkg checkout (used by CMake toolchain). Override locally if different.
VCPKG_ROOT=C:/Projects/vcpkg
