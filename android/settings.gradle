// Aleph One — Android (Phase 0 of the VR port). See docs/ANDROID_BUILD.md
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "AlephOne"
include(":app")
