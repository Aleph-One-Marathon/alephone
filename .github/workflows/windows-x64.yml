name: windows-x64

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v2
      - name: Build
        uses: ./.github/actions/build-windows
        with:
          platform: x64
          vcpkg_installed_folder: vcpkg_installed_64